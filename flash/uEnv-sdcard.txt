# SD-card appliance boot env (imported by the boot menu's `bootmmc`, which then bootz's).
# Root is the ext4 second partition, mounted read-only.
bootargs=console=ttyS2,115200n8 root=/dev/mmcblk0p2 rootfstype=ext4 ro rootwait
